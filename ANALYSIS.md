# CarParty.io 해부도

탑뷰 슈퍼카 레이싱 게임. 하나의 게임이 두 개의 독립 저장소로 갈라져 있고,
그 사이를 **결정론 시뮬레이션 코어 한 벌**이 붙들고 있다.
이 문서는 그 구조를 코드에서 직접 읽어 정리한 것이다.

| | |
|---|---|
| 저장소 | 2 (`carparty-client` / `carparty-server`) |
| JS 규모 | 14,509줄 |
| 권위 틱 | 60Hz |
| 분기점 | 공통 히스토리 `6ee5e21` 에서 갈라짐 |
| 넷코드 | v4 |
| 분석 시점 | 2026-08-27 · client `de2c317` · server `e47fe00` |

---

## 01. 두 저장소, 한 게임

`~/github/carparty` 자체는 git 저장소가 아니다. 그 안에 형제로 놓인 두 저장소가
프로젝트의 전부이고, 형제 폴더로 나란히 두는 것이 여러 기본값의 전제다.

| 저장소 | 역할 | 스택 | 규모 |
|--------|------|------|------|
| `carparty-client` | React UI 셸 + 캔버스 게임 엔진. 렌더 · 입력 · 예측 | React 18 · Vite 6 · ESM | 218 커밋 · 약 8,900줄 |
| `carparty-server` | WebSocket 릴레이 + 60Hz 권위 시뮬 + 계정 · 방 · 보스 AI | Node 20 · ws · Upstash Redis | 217 커밋 · 약 5,600줄 |

### 저장소가 갈라져도 물리는 한 벌이어야 한다

정본은 `carparty-server/sim.js` 하나뿐이고, 클라이언트의 `src/game/sim.js` 는
`scripts/sync-sim.mjs` 가 만드는 **생성물**이다 (`predev` · `prebuild` 에서 자동 실행).
차이는 상단 배너 3줄과 맨 끝 `export default SIM;` 뿐.
클라 사본을 손으로 고치면 물리가 갈라져 넷코드가 조용히 깨진다.

---

## 02. 전체 흐름

브라우저가 여는 것은 셸 문서이고, 게임은 그 안의 iframe 에서 돈다.
소켓 위로는 **위치가 아니라 버튼 비트**만 올라간다.

```
┌─ BROWSER ─────────────────────────────────────────────────────────────┐
│  index.html · shell.js   — PWA · safe-area · 포커스 유지 · 초대 링크    │
│  ┌─ IFRAME · play.html ───────────────────────────────────────────┐   │
│  │  ┌────────────┐   ┌──────────────────┐   ┌────────────────┐   │   │
│  │  │ React UI 셸 │──▶│    engine.js     │──▶│    sim.js      │◀──┼───┼──┐
│  │  │ 무상태·뼈대 │   │ 렌더·입력·예측·조정│   │   생성 사본     │   │   │  │
│  │  └────────────┘   └──────────────────┘   └────────────────┘   │   │  │
│  └────────────────────────────────────────────────────────────────┘   │  │
└───────────────────────────────────────────────────────────────────────┘  │
                          WEBSOCKET  /ws                                   │
       │ MSG_INPUT                              MSG_SNAP4 │                 │
       │ 틱 + 버튼 비트                          권위 스냅샷 │                 │
       ▼                                                  │                 │
┌─ NODE SERVER ─────────────────────────────────────────────────────────┐  │
│  ┌────────────────────────────┐  ┌────────────────────────────────┐  │  │
│  │ doTick() — 60Hz 권위 루프    │  │ Upstash Redis / users.json     │  │  │
│  │ 입력 소화→시뮬→판정→스냅샷   │  │ 계정 · 기록 · 칭호 · 친구        │  │  │
│  └────────────┬───────────────┘  └────────────────────────────────┘  │  │
│  ┌────────────▼───────────────┐  ┌────────────────────────────────┐  │  │
│  │ sim.js — 결정론 코어 (정본)  │  │ 방 · 매치메이킹 · 보스 AI        │  │  │
│  └────────────────────────────┘  └────────────────────────────────┘  │  │
└───────────────────────────────────────────────────────────────────────┘  │
                    npm run sync:sim — 빌드 시 복제 ───────────────────────┘
```

클라이언트와 서버가 *같은 파일*로 물리를 적분한다는 것이 이 구조의 전제다.
`sync:sim` 경로가 그 조건을 지키는 유일한 통로 — 정본은 판정 권위가 있는 서버 쪽에 둔다.

---

## 03. 넷코드 v4 — 시간 영역을 하나로

이 프로젝트에서 가장 많은 설계가 들어간 부분.
근거 문서는 `carparty-server/NETCODE.md` 이고,
"구현이 문서와 달라지면 문서를 먼저 고친다"고 스스로 선언한다.

### 고쳐야 했던 것

v3 는 **내 차는 클라이언트 권위, 상대 차는 스냅샷 보간**이었다.
내 차는 `t = now` 로, 상대 차는 `서버최신시각 − 보간지연` 즉 과거로 그려진다.
한 화면 안에 시간 영역이 둘이므로 속도 *v* 로 달리면 상대는 항상
`v × (편도지연 + 보간버퍼)` 만큼 뒤에 그려진다.
동기화 빈도를 아무리 올려도 사라지지 않는, 구조에서 나오는 오차다.

```
  v3 — 두 개의 시간 영역                 │   v4 — 하나의 시간 영역
                                        │
   표시        실제           내 차       │        표시 = 실제         내 차
  ┌╌╌╌┐      ┌───┐          █████       │         ┌═══┐            █████
  ╎   ╎      │   │          █████       │         │   │            █████
  └╌╌╌┘      └───┘          █████       │         └═══┘            █████
    └───────────┘                       │           │
        267px                           │        p95 ~1px
  차 7대 길이 · 1200km/h · 지연 100ms     │   상대를 내 예측 틱까지 전방 시뮬
                                        │
  양쪽 화면 모두에서 "내가 앞서 보인다"     │   모든 엔티티가 같은 틱에 그려진다
```

해법은 동기화를 촘촘히 하는 것이 아니라 **상대 차도 내 예측 틱까지 같은 물리로
밀어 그리는 것**이다. 남는 오차는 상대가 최근 RTT 안에 바꾼 입력뿐이고,
관성 큰 차 특성상 작아서 스무딩으로 흡수된다.

### netbot 실측 (봇 2대, 합성 지연 · 지터 주입)

| 지표 | @RTT 80ms | @RTT 300ms | 목표 |
|------|-----------|------------|------|
| 직선 고속 시점차 (p95) | ~1px | ~0px | < 30px |
| 상대 조향 전환 표시 오차 (p95) | 4px | 4.5px | 과도 < 45px |
| 스모 그라인딩 상대거리 차 (p95) | 3.4px | 9.8px | < 20px |
| reconcile 보정 | ~0 | avg 0.8px | 스냅 >2px < 1%/분 |
| 하드 리싱크 | 0 | 0 | 0 |
| 대역 (2인) | 하향 4.8KB/s · 상향 1.2KB/s | 동일 | < 15KB/s (8인) |

### 프로토콜

```
클라 → 서버   MSG_INPUT = 4        (바이너리 · 빅엔디언)
  u8  type          = 4
  u32 ackSnapTick   마지막으로 적용한 스냅샷 틱 (랙 보상용)
  u8  count         이 프레임의 입력 레코드 수
  [ u32 tick , u8 buttons ] × count
                    bit0 W · bit1 SPACE · bit2 A · bit3 D · bit4 S
                    bit5 PUNCH · bit6 RESTART

서버 → 클라   MSG_SNAP4 = 5        (60Hz)
  [클라별 10B 헤더]  type · tick · lastInputTick · phase(i8) · flags
  [그룹 공유 본문]   그룹당 1회만 인코딩 → 전원에 공유
                    id 0 = 보스 · 미포함 = 변화 없음 · 퇴장은 leave 이벤트로만

그 밖의 모든 것   chat · auth · room · race · boss · friend · title …
                 전부 JSON. 연결 직후 welcome{v:4}, 구 클라는 kicked{update}
```

수용 규칙: 틱 창 `[lastConsumed+1, serverTick+24]`, 같은 틱 재전송은 first-write-wins,
링버퍼 32슬롯, 프레임당 최근 3~6틱 중복 동봉(유실 공백 메움).

### 핵심 파라미터

| 항목 | 값 | 코드 위치 |
|------|-----|-----------|
| 고정 틱 | 60Hz | `SIM.TICK_RATE` |
| 클라 예측 틱 | `P = 추정 서버틱 + lead` (1~20) | `engine.js · notePhase` |
| lead 적응 신호 | 스냅샷 헤더 phase — 2초 창 2등 최대값 | `engine.js · clock` |
| CCD 서브스텝 | 4 (상수) | `SIM.FIXED_SUBSTEPS` |
| 결정론 격자 | x,y → ¼px · v → ⅛px/s · angle → i16 · steer → i8 | `sim.js · quantize()` |
| 렌더 수렴 | errorOffset 감쇠, 150px / 25° 초과 시 스냅 | `engine.js · errOff` |
| 입력 기아 | 0~4틱 유지 → 5틱 중립 코스트 → 45틱 하드 프리즈 | `server.js · STARVE_*` |
| 키프레임 | 120틱 (2초) | `server.js · KEYFRAME_TICKS` |
| 백프레셔 | 64KB 스킵 / 1MB 종료 | `server.js · BACKPRESSURE_*` |
| 랙 보상 | 고정 35ms 되감기 | `server.js · rewindOf` |

---

## 04. 서버

CommonJS · Node 20 · 의존성 셋 (`ws`, `@upstash/redis`, `dotenv`).
`server.js` 한 파일이 3,222줄로 거의 전부를 담는다.

### doTick() — 매 틱 네 단계

```
1. 그룹 구성    mode:<mode> 또는 room:<id>
               관전 · 사망 대기처럼 그룹 밖인 접속도 입력 링은 소화한다
               (안 하면 32슬롯 링이 넘쳐 이후 입력이 통째로 버려진다)

2. 입력 + 시뮬  SIM.stepGroup(entries, env, { collide, contacts })
               충돌 활성 = plaza · survival · sumo · boss + 레이스 중인 pro 방

3. 규칙 판정    히스토리 적립 → 타임어택 attackStep → 스모 링아웃
               → 프로 랩 게이트 → runCollisions(헤드킬) → sumoTick(펀치)

4. 스냅샷      그룹 본문 1회 인코딩 → 클라별 10B 헤더 붙여 전송
```

루프 자체는 `simLoop()` 의 hrtime 드리프트 보정 `setTimeout` 체인이다.
밀린 틱이 6틱을 넘으면 "히치"를 선언해 밀린 분을 버리고 시간을 재정렬한 뒤
전원에게 키프레임을 다시 보낸다 — 이벤트루프 스톨이 버스트 시뮬과 일제 보정으로
번지는 것을 막는다.

### server.js 지형

| 줄 | 내용 |
|----|------|
| ~131–390 | 계정 — Redis/파일 저장, scrypt 해시, 토큰, 기록 필드, 통계 |
| ~389–470 | 정적 파일 서버 + `/healthz` |
| ~471–760 | v4 코어 — 그룹 판정, MODE_ENV, 입력 수신/소화, 스냅샷 인코딩 |
| ~761–960 | **doTick() / simLoop()** — 심장 |
| ~958–1490 | WebSocket 연결 핸들러 (JSON 메시지 30여 종) |
| ~1607–2010 | 방 수명주기 · 매치메이킹 · 점수 반영 |
| ~2013–2270 | 소셜 — 친구, 귓속말 히스토리, 칭호 14종 |
| ~2272–2530 | 관리자 명령 (`/추방` `/차단` `/기록삭제` …) |
| ~2679–3210 | 보스전 — 아레나 · 스킬 · AI · 60Hz bossTick, 스모 틱 |

### 계정과 데이터

- **저장소** — Upstash 환경변수가 있으면 Redis, 없으면 `users.json` 폴백.
  같은 키 스킴을 CLI 도구들도 그대로 쓴다.
- **비밀번호** — 클라가 `sha256("carparty:v1:" + id + pw)` 로 선해시해 보내고,
  서버가 그 위에 scrypt 를 얹는다. 원문은 서버에 도달하지 않는다.
- **모드** — 타임어택 12코스 · `pro`(커스텀/경쟁전/일반전) · `survival` · `sumo` ·
  `boss` · `plaza`. `lobby` 와 `soccer` 는 로컬 전용이라 소켓을 타지 않는다.
- **운영 도구** — `tools/` · `scripts/` 에 계정 · 기록 · 채팅 · 랭크 조회 CLI.
  대부분 읽기 전용이라 서버 실행 중에도 안전하다.

---

## 05. 클라이언트

React 는 **화면의 뼈대만** 그린다. 그 안을 채우고 보이고 숨기는 일은
6,880줄짜리 캔버스 엔진이 `id` 로 요소를 잡아 명령형으로 처리한다.

60Hz 물리/렌더 루프를 React 상태에 태우면 프레임마다 리렌더가 돌아 오히려 느려진다.
그래서 `App` 트리에는 상태가 없고, 최초 1회 마운트 뒤 React 는 DOM 을 다시 건드리지 않는다 —
엔진이 나중에 넣은 자식(채팅 줄 · 순위 행 · 칭호 칩)도 그래서 안전하다.
엔진은 `useGameEngine` 의 동적 import 로 "DOM 커밋 이후"에 부팅된다.

### 문서가 둘인 이유

브라우저가 여는 것은 `index.html` 이지만 게임은 그 안 iframe 의 `play.html` 에서 돈다.
개발자도구 콘솔은 기본적으로 최상위 문서에서 평가되므로, 게임을 한 겹 안에 두면
합성 키를 쏘거나 게임 DOM 을 건드리려 할 때 컨텍스트를 프레임으로 바꾸는 단계가 하나 더 붙는다.
**벽이 아니라 문턱이다.** 경계를 넘는 것은 `postMessage` 로만 오간다 —
초대 링크 `?room=`, safe-area 값, 포커스, 하트비트.

### engine.js 지형

| 줄 | 섹션 |
|----|------|
| ~48 | WORLD — 모드 · 월드 크기 정의 |
| ~329 | SFX — 외부 파일 없이 WebAudio 로 즉석 합성 |
| ~1038 | 물리 어댑터 — 키→버튼 비트, env 조립, 시뮬 이벤트→SFX |
| ~1451 | 렌더 · 카메라 · 스키드 · 폭발 |
| ~1685–2750 | 보스전 · 광장 · 스모 연출 |
| ~2751 | 축구 (베타 · 싱글 · 3D 롤링 공) |
| ~3902 | 멀티플레이 — WS 연결 · JSON 핸들러 |
| ~5098 | **넷코드 v4 클라** — 시계 동기 · 예측 · 조정 · 전방 시뮬 |
| ~5510 | 메인 루프 — 고정 틱 accumulator + 렌더 전용 부분 스텝 |
| ~6378 | 로그인 · 대시보드 · 랭킹 |

메인 루프는 정확히 60Hz 로만 적분하고, 남는 시간은 **사본 상태 + 라이브 키**로
렌더 전용 부분 스텝을 밟는다. 144Hz 에서도 매끈하고 새 입력이 다음 프레임에 바로
보이면서, 결정론은 훼손되지 않는다.

---

## 06. 치트 방어

층은 넷이고 **아래로 갈수록 실제로 막는 힘이 세다.**
위쪽 셋은 비용을 올리는 문턱이지 벽이 아니다 — 코드 주석이 그렇게 명시하고 있다.

| 층 | 막는 것 | 위치 |
|----|---------|------|
| 1. iframe 격리 | 콘솔에서 게임 문서에 곧장 손대는 것 | `src/shell.js` · `src/security/guard.js` |
| 2. 난독화 + 소스맵 미배포 + CSP | 프로토콜 역독해로 봇 제작. `script-src 'self'` 로 주입 스크립트와 eval 차단 | `vite.config.js` · `public/_headers` |
| 3. `isTrusted` 게이트 + 네이티브 선점 | 페이지 안 스크립트의 합성 키. WebSocket · 시계 · rAF 원본 참조를 엔진보다 먼저 확보 | `src/security/guard.js` |
| 4. **서버 권위 시뮬** | **속도핵 · 순간이동 · 기록 위조 — 애초에 표현할 수단이 없다** | `server.js` · `sim.js` |

그래서 남는 치트는 하나뿐이다 — **사람 대신 프로그램이 버튼을 누르는 것.**
봇은 규칙을 어기지 않는다. 사람이 낼 수 없는 정밀도로 정직하게 몰 뿐이다.

### ta-input.js — 손가락의 물리적 한계로 가른다

판정 근거는 주행 궤적이 아니라 조향 입력의 모양이다. 사람은 조향키를 한 번 누르면
최소 수십 ms 는 붙잡고 있는데, 봇은 틱 단위 최적 제어를 하므로 1~2틱(16~33ms)짜리
조향이 무더기로 나오고 전환이 초당 수십 번 일어난다.

| 판정 | 조건 | 처리 |
|------|------|------|
| `ok` | 표본 < 20 홀드 이거나 임계 미달 | 그대로 저장 |
| `suspect` | flipRate ≥ 16/s && flickFrac ≥ 0.50 | 저장 + `ta-suspect.jsonl` 기록 + 관리자 알림 |
| `impossible` | flipRate ≥ 25/s && flickFrac ≥ 0.75 | 기록 반려 + 관리자 알림 + 본인 통지 |

자동밴은 없다. 제재는 관리자 수동 판단이고, 임계값은 실제 로그를 보고 조이거나 푼다 —
**오탐이 치터 한 명 놓치는 것보다 훨씬 손해**라는 판단이 코드에 명시되어 있다.

---

## 07. 배포

브라우저 입장에서 소켓 주소는 언제나 같은 오리진의 `/ws` 다.
그것을 실제 게임 서버로 넘기는 주체만 환경마다 다르다.

| 환경 | `/ws` 를 넘기는 주체 | 설정 |
|------|---------------------|------|
| 개발 | Vite 개발 서버 프록시 | `GAME_SERVER` |
| Cloudflare Pages | Pages Function `functions/ws.js` | `GAME_SERVER_URL` |
| Render Static | 없음 — 프록시 불가 | `VITE_WS_URL` 필수 |
| 서버가 직접 서빙 | 없음 — 서버가 `/ws` 를 스스로 받는다 | — |

`_redirects` 로는 WebSocket 프록시를 못 한다 — 101 업그레이드를 다루지 못하므로
Pages Function 이 필요하다. 핸드셰이크만 함수를 거치고 이후 프레임은 Cloudflare 가
직접 파이프하므로, 60Hz 트래픽에도 메시지마다 함수가 깨어나지 않는다.

### 게임 서버가 Workers 로 못 가는 이유

| 서버가 하는 일 | Workers 에서의 문제 |
|---------------|---------------------|
| `new WebSocketServer({ server })` | Workers 는 `WebSocketPair` 를 쓰고, 연결을 붙들려면 Durable Object 가 필요 |
| 모듈 최상단 60Hz `setInterval` | 요청 사이에 살아 있는 프로세스가 없다. DO 알람으로 60Hz 는 비현실적 |
| 전 접속 공유 메모리 상태 | 인스턴스가 요청마다 갈릴 수 있어 단일 DO 로 몰아야 한다 |
| `fs` · `crypto.scryptSync` | 파일시스템이 없고 scryptSync 는 workerd 에 없다 |

결론은 **서버는 Render 에, 클라만 Pages 에** — 지금 구조 그대로다.
Render 무료 플랜은 15분 미접속 시 잠들고 콜드스타트가 수십 초 걸리는데,
클라이언트 자동 재접속이 그걸 흡수한다.

---

## 08. 어긋난 부분

코드를 읽으면서 실제로 걸린 것들. 대부분 기능을 깨뜨리지는 않지만,
문서와 코드가 다른 말을 하는 지점이라 다음에 만질 때 함정이 된다.

### ① wrangler.jsonc 주석이 정반대를 지시했다 — **수정됨**

`carparty-client/wrangler.jsonc`

"★ VITE_WS_URL 을 반드시 빌드 환경변수로 넣어야 한다"고 적혀 있었지만,
`functions/ws.js` 가 들어온 뒤로 Pages 에서 이걸 넣으면 프록시를 *우회*한다.
README · vite.config.js · functions/ws.js 셋 다 "넣지 말라"는 쪽이라 주석만 남은 유물이었다.

→ "넣지 않는다"로 뒤집고, 이유(Pages Function 이 /ws 를 넘긴다)와
탈출구로 쓰는 경우(프록시 없는 호스팅), 업스트림 변경은 `GAME_SERVER_URL` 임을 명시했다.

### ② 멀티플레이 섹션 배너가 v3 설명 그대로였다 — **수정됨**

`carparty-client/src/game/engine.js:3902`

"자기 차량 상태(x, y, angle)를 30Hz 로 서버에 전송한다"고 적혀 있었는데 v4 는 입력만 보낸다.
바로 아래 코드는 정상적인 v4 였고 주석만 갱신이 누락된 것 — 그 파일을 처음 읽는 사람이
제일 먼저 믿을 문장이라 위험도가 낮지 않았다.

→ 배너를 v4 기준으로 다시 쓰고(입력만 전송 / 상대는 전방 시뮬 / 이 절의 실제 역할은
연결 관리와 JSON 분기), 같이 낡아 있던 `remotePlayers` 주석(`tx/ty/tangle` 보간 목표)과
그 아래 고아로 남아 있던 v3 "바이너리 프로토콜" 배너도 함께 정리했다.

### ③ Render 정적 배포의 SPA 폴백이 README 가 경고한 그 위험을 갖고 있었다 — **수정됨**

`carparty-client/render.yaml`

README 는 Pages 에 `_redirects` 를 두지 않은 이유로
"없는 `.js` 요청에 HTML 이 200 으로 돌아와 서비스워커 캐시가 오염된다"를 든다.
그런데 `render.yaml` 의 `/* → /index.html` 리라이트가 Render 쪽에 같은 조건을 만들었다.

→ `routes` 블록을 제거했다. 이 앱은 경로 라우팅을 쓰지 않고(초대는 쿼리 `?room=`),
게임 문서도 실제 파일 `/play.html` 로 열리므로 폴백 자체가 필요 없다.
같은 판단을 주석으로 남겨 다음에 다시 추가되지 않게 했다.

### ④ 죽은 상수 — **수정됨**

`carparty-server/server.js:471`

`MSG_SNAPSHOT` · `MSG_SNAPSHOT3` 은 선언만 있고 참조가 없었다.
살아 있는 레거시 경로는 `MSG_STATE` 하나뿐이고, 그것도 "구 클라 감지 → 새로고침 유도" 용도다.

→ 두 상수와 같은 블록에서 함께 죽어 있던 헬퍼 `rgbToHex` · `sendBin` 까지 제거하고,
`MSG_STATE` 에는 왜 혼자 남았는지를 주석으로 붙였다. 절 배너도 v3 시절 설명
("클라→서버 state / 서버→클라 snapshot")에서 v4 기준으로 다시 썼다.

### ⑦ 죽은 상태 — `net.pendingTeleport` — **수정됨**

`carparty-client/src/game/engine.js`

8곳에서 `true` 로 대입되지만 **읽는 곳이 한 군데도 없었다.** 코드 자신도
"(v4 잔재 — 텔레포트는 서버 spawn 이벤트가 처리. 대입만 남음)"이라고 적어 두었다.

→ 필드 선언 1줄 + 대입 8줄, 총 9줄을 제거했다. 읽는 쪽이 없어 동작 변화는 없다.

### ⑧ `net` 의 남은 쓰기 전용 필드 세 개 (미처리)

`carparty-client/src/game/engine.js`

⑦ 를 지우면서 같은 객체를 훑다가 찾았다. 셋 다 대입만 있고 읽는 곳이 없다.

| 필드 | 대입 위치 | 원래 용도로 보이는 것 |
|------|-----------|----------------------|
| `net.rttMs` | pong 핸들러 | ping/pong 왕복 지연(ms) |
| `net.lastSnapAt` | `applySnap4` | 마지막 스냅샷 도착 시각 |
| `net.lastInputAck` | `applySnap4` | 서버가 소화한 내 마지막 입력 틱 |

`pendingTeleport` 와 달리 이쪽은 **넷코드 계측값**이라, 디버그 HUD 에 물리려고 미리
잡아 둔 자리일 가능성이 있다. 각각 한 줄이라 지우기도 되살리기도 쉽다.
(같은 이름의 `net.lastSnapTick` 은 `sendInputFrame` 이 실제로 읽으므로 살아 있다.)

### ⑤ 의도된 편차 — 문서에 이미 기록되어 있다

`carparty-server/NETCODE.md §11`

스냅샷 델타 마스크는 프로토콜에만 있고 현재는 전 필드를 매 틱 보낸다(대역이 목표 안이라 보류).
보스는 `stepBoss` 공유화 대신 ballistic 외삽, 랙 보상은 "시점 재구성" 대신 고정 35ms 되감기,
카메라는 스프링 대신 하드락. 넷 다 이유와 함께 적혀 있다.

### ⑥ 아직 구현되지 않은 설계

`carparty-server/NETCODE.md §7` · `ta-input.js`

TOP10 신기록의 입력 스트림 영속과 재시뮬 검증(§7)은 미구현이다.
지금 봇 방어는 온라인 휴리스틱(`ta-input.js`) 하나뿐이라,
리플레이 기반 사후 검증이라는 두 번째 그물이 없다.

### ⑨ 클라이언트 README 의 조작키 설명이 틀렸다 (미처리)

`carparty-client/README.md`

> "WASD 와 방향키가 **둘 다** 동작한다. 설정 → 조작키 에서 한쪽만 쓰도록
> 좁힐 수 있다 (WASD / 방향키 / 둘 다)."

코드는 그렇지 않다. `CONTROL_SCHEMES = ["wasd", "arrows"]` 로 **한 번에 한 스킴만**
동작하고 기본은 `wasd` 다. 옛 `"both"` 저장값은 `normScheme()` 이 `wasd` 로 접는다.
커밋 `4c42739`("방향키로도 운전할 수 있게")에서 쓰인 설명이
`05db2ac`("조작키 단일화")에서 갱신되지 않았다.

지금까지 찾은 것 중 유일하게 **플레이어가 직접 겪는 오해**다 — 방향키만 눌러 보고
"안 되네" 하고 나가는 사람이 생긴다. 나머지는 전부 개발자만 보는 주석·설정이었다.

---

*근거 문서: `carparty-server/NETCODE.md` · 양쪽 `README.md` · 소스 직접 확인*
